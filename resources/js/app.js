/**
 * Entry point utama JS untuk WebTransaksi
 * di-load otomatis oleh Vite di Blade layout
 */

import './bootstrap';

document.addEventListener('DOMContentLoaded', () => {
    console.log('%c🔥 WebTransaksi App.js Loaded', 'color: #4F46E5; font-weight: bold;');
});
